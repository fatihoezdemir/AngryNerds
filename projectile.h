#ifndef PROJECTILE_H
#define PROJECTILE_H
#include "dynamicobject.h"
/*
class Projectile : public DynamicObject, public QGraphicsItem
{
public:
    explicit Projectile(const QPixmap &pixmap, QPointF pos = QPointF(0.0,0.0), b2World* world = 0, QGraphicsItem* parent = nullptr);
};
*/
#endif // PROJECTILE_H

